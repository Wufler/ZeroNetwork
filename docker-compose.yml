services:
 postgres:
  image: postgres:18-alpine
  ports:
   - 5432:5432
  volumes:
   - pgdata:/var/lib/postgresql
  environment:
   - POSTGRES_PASSWORD=postgres
   - POSTGRES_DB=postgres

volumes:
 pgdata:
